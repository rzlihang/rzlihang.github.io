# rzlihang.github.io

Personal site content built with Astro.

## Development

Install dependencies:

```sh
pnpm install
```

Run the local development server:

```sh
pnpm dev
```

Build the site:

```sh
pnpm build
```

Preview the production build locally:

```sh
pnpm preview
```
